package com.fitouts.subcontractor.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ScWhatsappOutboxRepository extends JpaRepository<ScWhatsappOutbox, Long> {
}
