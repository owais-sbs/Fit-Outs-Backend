-- Catalog rows and Dubai jurisdiction packs are seeded per company from
-- classpath:seed/vetrobuild_erp_seed_v1.json by ApprovalConfigSeedService
-- when Approvals Config is first opened (or when a project is created).
SELECT 1;
